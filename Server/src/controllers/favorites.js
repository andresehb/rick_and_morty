const axios = require('axios');
require('dotenv');
const URL = process.env.SERVER_URL
const STATUS_OK = 200;
const STATUS_ERROR = 500;

const getFav = (req, res) => {};

module.exports = {
    getFav
};